# 팀 프로젝트 전 일정 사항 (변동 가능성 O)

1. 3월 27일 - 3개 과목 시험 및 팀 공지 / 회식
2. Back-Front 연결 이후 시험 사항
> 1. OAuth2(구글 로그인 기준) 구형
> 2. AWS(Amamzon Web Service) 기준 배포(Springboot) 

# 팀 프로젝트 이후 사항
1. 4월 첫째주 경 초기 기획 발표(주제 발표 및 주제 선정 배경 / 구현 기능 등)
2. 강사 피드백 후에 같은 PT(혹은 추가자료)로 재발표(서로 다른 발표자가 발표할 것).
3. 팀 프로젝트 시작

# PUT 요청(Modal에 기존 정보가 그대로 있을때)
1. 각 테이블의 row에 현재 delete 버튼만 존재하는데, Edit 버튼을 추가하여 수정을 기능을 구현한다. -> 그렇다면 어느 컴포넌트에서 Edit 버튼이 필요한가. 어떻게 설정할 수 있는가를 고민해야한다.

2. 해당 버튼을 눌렀을때 사용자가 기존 자동차를 편집하고 변경사항을 저장할수 있도록 모달이 열려야 한다. 그리고 그 모달창은 POST 요청과 동일한 템플릿이 될건데, 차이점이 있다면 특정 row의 Edit 버튼을 눌렀을때 활성화 되는 Dialog 컴포넌트 에는 수정 대상 차량의 정보가 미리 입력되어있도록 한다.(필요한 부분만 수정될수 있도록)

3. components 폴더에 EditCar.tsx 컴포넌트 생성 후 초기화
```tsx
import { Car,CarResponse } from "../types"
import { useState } from "react"

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({cardata} : CarResponse) {
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  })


  return(
    <>
    
    </>
  )
}
```

- 이상의 코드에서 다양한 설명이 있다. AddCar.tsx의 경우 href(고유값)이 아직 생성되지 않았기 때문에 props drilling이 요구되지않지만 EditCar.tsx에는 특정 자동차 정보가 있고 그 자동차 정보를 수정하는 것이기 떄문에 객체 구조분해를 활용해야하고, 그 자료형이 CarResopnse이기 때문에 `FormProps`라는 타입을 별개로 정의했다.

- 하지만 FromProps의 경우 EditCar 컴포넌트 내부에서만 사용될것이기 때문에 굳이 types.ts로 분리 해주지않았다. 즉 재사용을 하게 될 자료형이라면 별개의 파일에사 export 하겠지만 단일한 곳 에서만 사용된다면 굳이 외부파일에서 import 해올 필요가 없다는 의미가 된다.

- 그리고 상태를 선언해서 `<Car>`로 작성했기 때문에 AddCar 컴포넌트의 상태를 그대로 복사해왔다. 차 한대 추가 / 차 한대 수정에서 '차 한대' 라는 공통점을 가지고 있고, state의 자료형이 `<Car>`로 동일하기 때문에 복사해와도 문제가 없을것 같다.
```tsx
import { Car,CarResponse } from "../types"
import { useState } from "react"
import { Dialog, DialogActions, DialogTitle,  } from "@mui/material";
type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({cardata} : CarResponse) {
  const [ open,setOpen ] = useState(false); 
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  })

  const handleClickOpen = () => {
    setOpen(true)
  }
  const handleClickClose = () => {
    setOpen(false)
  }

  const handleSave =() => {
    setOpen(false)
  }
  return(
    <>
      <button onClick={handleClickOpen}>
      Edit
      </button>
      <Dialog open={open} onClick={handleClickClose}>
        <DialogTitle>Edit Car</DialogTitle>
        <DialogActions>
          <button onClick={handleClickClose}></button>
          <button onClick={handleSave}></button>
        </DialogActions>
      </Dialog>
    </>
  )
}
```

- 현재까지의 작성 영역을 봤을때, DialogTitle과 DialogActions 사이에 수정할 내용을 입력받을 input 창들이 필요하다는 것을 알수있다. 그리고 우린 AddCar에서 가지고 오기로 했다.

- 현재 타이밍에서 input창들을 모듈화 해야겠다는 생각이 든다.
- 그러면 EditCar 부분을 잠깐 멈추고, 모듈화 한다음에, 완성된 input창들 (즉 form)을 export 시켜서 EditCar에서 그냥 그대로 가져와 재사용 하도록 한다.

- NewCar 폼의 텍스트 필드를 렌더링 하는 새로운 컴포넌트를 생성, 그리고 New Car와 Edit 폼 모두에서 이용할수 있도록 한다. components 폴더 내에 CarDialogContent.tsx를 생성

```tsx
// CarDialogContent.tsx

import { Car } from "../types";
import { DialogContent } from "@mui/material";
type DialogFormProps = {
  car: Car;
  handleChange: (event: React.ChangeEvent<HTMLInputElement>) => void;
}

export default function CarDialogContent({car, handleChange }: DialogFormProps) {
  return(
    <>
      <DialogContent>
          <input type="text" placeholder="Brand" name="brand" value={car.brand} onChange={handleChange}/><br />
          <input type="text" placeholder="Model" name="model" value={car.model} onChange={handleChange}/><br />
          <input type="text" placeholder="Color" name="color" value={car.color} onChange={handleChange}/><br />
          <input type="text" placeholder="RegistrationNumber" name="registrationNumber" value={car.registrationNumber} onChange={handleChange}/><br />
          <input type="number" placeholder="ModelYear" name="modelYear" value={car.modelYear} onChange={handleChange}/><br />
          <input type="number" placeholder="Price" name="price" value={car.price} onChange={handleChange}/><br />
      </DialogContent>    
    </>
  )
}
```

```tsx
import { Dialog, DialogContent, DialogTitle, DialogActions } from "@mui/material"
import { Car } from "../types"
import { useState } from "react"
import { addCar } from "../api/carapi";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import CarDialogContent from "./CarDialogContent";

export default function AddCar() {
  const queryClient = useQueryClient();

  const { mutate } = useMutation(addCar, {
    onSuccess: () => {
      queryClient.invalidateQueries(['cars']);
    },
    onError: (err) => console.log(err),
  });

  const [ open, setOpen ] = useState(false);
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  });
  
  const handleClickOpen = () => setOpen(true);
  const handleClickClose = () => setOpen(false);

  const handleChange = (event:React.ChangeEvent<HTMLInputElement>) => {
    setCar({...car, [event.target.name]: event.target.value})
  }

  const handleSave = () => {
    mutate(car);    // carapi에 있는 addCar() 함수에 해당
    setCar({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
    })
    handleClickClose();
  }


  return (
    <>
      <button onClick={handleClickOpen}>New Car</button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>New Car</DialogTitle>
        <CarDialogContent car={car} handleChange={handleChange} />
        <DialogActions>
          <button onClick={handleClickClose}>취소</button>
          <button onClick={handleSave}>저장</button>
        </DialogActions>
      </Dialog>
    </>
  )
}
```

- 이상의 두개의 파일에서 수정했다. AddCar에서 `<DialogContent>` 컴포넌트 부분을 다 달라서 CarDialogContent 컴포넌트의 return 부분에 집어넣었다. 그러면서 `{car, handleChange}`를 집어넣어놨고, AddCar에서도 props drilling을 해줘야 하기 때문에 `<CarDialogContent car={car} handleChange={handleChange} />`와 같은 return문 부분이 추가 되었다. `car=` 부분은 props drilling, `{car}`부분은 상태 및 함수에 해당한다는 것을 다시한번 확인해준다.

```tsx
// carlist.tsx

import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { getCars, deleteCar } from "../api/carapi";
import { DataGrid, GridColDef, GridCellParams } from "@mui/x-data-grid";
import { Snackbar } from "@mui/material";
import { useState } from "react";
import AddCar from "./AddCar";
import EditCar from "./EditCar";

export default function Carlist() {
  const [ open, setOpen ] = useState(false)
  const queryClient = useQueryClient();


  const { data, error, isSuccess } = useQuery({
    queryKey: ['cars'],
    queryFn: getCars
  });

  const { mutate } = useMutation({
    mutationFn: deleteCar, 
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cars'] });
      setOpen(true);
    },
    onError: (err) => {
      console.error(err);
    },
  });

  const columns: GridColDef[] = [
    { field: 'brand', headerName:'Brand', width: 200, } ,
    { field: 'model', headerName:'Model', width: 200, },
    { field: 'color', headerName:'Color', width: 200, },
    { field: 'registrationNumber', headerName:'RegistrationNumber', width: 200, },
    { field: 'modelYear', headerName:'ModelYear', width: 200, },
    { field: 'price', headerName:'Price', width: 200, },
    {
      field: 'edit',
      headerName: '',
      sortable: false,
      filterable: false,
      disableColumnMenu: true,
      renderCell: (params:GridCellParams) => <EditCar cardata={params.row}/>
    },
    {
      field: 'delete',
      headerName: '',
      sortable: false,
      filterable: false,
      disableColumnMenu: true,
      renderCell: (params:GridCellParams) => {
        return <button 
          onClick={() => {
            if (confirm(`${params.row.brand}의 ${params.row.color} ${params.row.model}을 삭제 하시겠습니까?`)) {
              mutate(params.row._links.self.href);
            }
          }}
        >
        삭제
        </button>
      }
    }
  ]

  if (!isSuccess) {
    return <span>Loading...</span>
  } else if (error) {
    return <span>자동차 데이터를 가져오던 중 오류가 발생했습니다. </span>
  }
  else {
    return(
      <>      
      <AddCar />
      <DataGrid
        rows={data}
        columns={columns}
        disableRowSelectionOnClick={true}
        getRowId={row => row._links.self.href}
      />
      <Snackbar
        open={open}
        autoHideDuration={2000}
        onClose={() => setOpen(false)}
        message='차량 정보가 삭제 되었습니다. 🗑️'
      />
      </>
    )
  }
}
```

```tsx
import { Car,CarResponse } from "../types"
import { useState } from "react"
import { Dialog, DialogActions, DialogTitle,  } from "@mui/material";
import CarDialogContent from "./CarDialogContent";

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({cardata} : FormProps) {
  const [ open,setOpen ] = useState(false); 
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  })

  const handleClickOpen = () => {
    setCar({
      brand: cardata.brand,
      model: cardata.model,
      color: cardata.color,
      registrationNumber: cardata.registrationNumber,
      modelYear: cardata.modelYear,
      price: cardata.price
    })
    setOpen(true)
  }
  const handleClickClose = () => {
    setOpen(false)
  }

  const handleSave =() => {
    setOpen(false)
  }

  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    setCar({...car, [event.target.name]: event.target.value})
  }

  return(
    <>
      <button onClick={handleClickOpen}>
      Edit
      </button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>Edit Car</DialogTitle>
        <CarDialogContent car={car} handleChange={handleChange} />
        <DialogActions>
          <button onClick={handleClickClose}>Cencel</button>
          <button onClick={handleSave}>Save</button>
        </DialogActions>
      </Dialog>
    </>
  )
}
```

- 이상에서 중요한 부분은 handleClickOpen 함수 내에 setCar()를 통해서 특정 row의 차량 정보를 car 상태에 저장한 부분이다. 이상의 함수를 통해서 모달이 열리기전에 특정 차량상태가 저장되고, textField에 표시되게 된다.

- 그리고 수정창이 열리는 것 까지는 구현했는데 handleSave 부분에 차량 정보를 업데이트 하고 DB에 저장되는 부분이 구현이 안됐다.

- axios 활용하고 put요청을 통해서 `http://localhost:8080/api/vehicles/[특정자동차id]`에 보내야한다. 즉 deleteCar와 동일한 url이 된다. 하지만 deleteCar의 경우엔 url만 argument로 잡으면 되는 반면에, 수정 내용도 전달되어야 하기 때문에 addCar() 함수와 유사한 점도 있다.

- url도 보내야하고, 변경사항도 보내야하기 때문에 추가 type를 설정한다. types.ts에 CarEntry 자료형을 정의
  - > 이유가 있는데 react query에서 커스텀 함수의 경우 매개변수를 하나밖에 못 받아서 그렇다. `(매개변수1: 자료형1, 매개변수2:자료형2) => {구현부}` 이렇게가 불가능하다는 의미이다.

```tsx
import { useState } from "react";
import { CarResponse, Car, CarEntry } from "../types";
import { Dialog, DialogActions, DialogTitle } from "@mui/material";
import CarDialogContent from "./CarDialogContent";
import { updateCar } from "../api/carapi";
import { useMutation, useQueryClient } from "@tanstack/react-query";

type FormProps = {
  cardata: CarResponse;
}

export default function EditCar({ cardata } : FormProps) {
  const [ open, setOpen ] = useState(false);
  const [ car, setCar ] = useState<Car>({
    brand: '',
    model: '',
    color: '',
    registrationNumber: '',
    modelYear: 0,
    price: 0
  });

  const queryClient = useQueryClient();
  const { mutate } = useMutation(updateCar, {
    onSuccess: () => {
      queryClient.invalidateQueries(['cars']);
    },
    onError: (err) => {
      console.log(err);
    }
  });

  const handleClickOpen = () => {
    setCar({
      brand: cardata.brand,
      model: cardata.model,
      color: cardata.color,
      registrationNumber: cardata.registrationNumber,
      modelYear: cardata.modelYear,
      price: cardata.price
    });
    setOpen(true);
  }

  const handleClickClose = () => {
    setOpen(false);
  }

  const handleSave = () => {
    const url = cardata._links.self.href;
    const carEntry : CarEntry = {car, url};
    mutate(carEntry);
    setCar({
      brand: '',
      model: '',
      color: '',
      registrationNumber: '',
      modelYear: 0,
      price: 0
    });
    setOpen(false);
  }

  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    setCar({...car, [event.target.name]: event.target.value});
  }

  return(
    <>
      <button onClick={handleClickOpen}>
        Edit
      </button>
      <Dialog open={open} onClose={handleClickClose}>
        <DialogTitle>Edit Car</DialogTitle>
        <CarDialogContent car={car} handleChange={handleChange} />
        <DialogActions>
          <button onClick={handleClickClose}>Cancel</button>
          <button onClick={handleSave}>Save</button>
        </DialogActions>
      </Dialog>
    </>
  );
}
```

- useMutation() / useQueryClient의 사용과 관련된 부분을 복습하면 useQuery()와 useMutation()의 차이가 있다.

# CSV로 데이터 내보내기
- AG-Grid가 아니라 x-data-grid를 도입하는 이유는 얘가 기본적으로 csv로 보내기 기능을 지원하기 때문이였다.

- carfront2 복사 후, github push하기